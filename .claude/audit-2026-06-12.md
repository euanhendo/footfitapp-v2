# Walkthrough audit — 2026-06-12

Raw screen-by-screen feedback from the user's phone walkthrough, triaged.
Working order: change → /verify → device check. Living doc — more feedback expected.

## P1 — Credibility & correctness (no new assets needed)

1. ✅ **Score breakdown speaks human** (Result). Shipped `682310f`. Today: "your length 269, boot 240–299" —
   meaningless range (reads like the whole size run, not the recommended size).
   Wanted: the recommended size's max length, spare room translated into toe-room
   language (e.g. "~half a size of room"), width version too: where you sit vs the
   boot's max width, plus comfort guidance ("snug — will stretch over time, may be
   uncomfortable at first").
2. **Show estimated foot measurements for basic inputs** (Result/profile card).
   User entered UK 8.5 + wide and got nothing back. Wanted: the mm range the app
   already derives internally (e.g. wide ≈ 110–102, standard ≈ 102–95 — use the real
   numbers from `lib/fitting.ts`, not these guesses). Pair with a persistent-but-polite
   scanner nudge: "scan with your phone for more accurate results."
   Builds on `measureSource` provenance (a3b0c16).
3. ✅ **Buy link goes to the retailer, not Google** (Result → boot). Shipped `7b1ac97`.
   All 61 boots now use brand-site search URLs (e.g. `nike.com/gb/w?q=phantom+gx+ii+elite`) —
   one stable pattern per brand, lands on the model. Exceptions: Mizuno → Pro:Direct
   Morelia silo (Mizuno EMEA blocks validation), Canterbury → its boots category page
   (site has no search endpoint), Saucony → family query ("ride", versioned queries
   return zero). Per-product retailer search URLs all 404'd — pattern documented so
   nobody retries it. (Full multi-retailer price comparison: see Deferred.)
4. **Audit sock thickness values** (`sockDatabase.json`). "Make sure the extra mm the
   sock adds is correct" — verify against manufacturer/published data.

## P2 — Flow restructure

5. ✅ **ManualInput → step-by-step wizard.** Shipped `d0aa713` (round 2 of user
   feedback): title/subtitle/guide-link removed, scan card on top, then progressive
   steps — input method → size system → size entry → width feel → estimated-feet
   card (covers audit item 2's estimated-mm + scanner nudge). MeasureGuideScreen
   still routed but no longer linked.
   _Partially shipped `786d827`_: scan-first now happens at app open — new users get
   a Welcome page (scan CTA + skip); onboarding scans hand measurements to Home,
   which routes straight to SockSelection on sport tap. Home also reworked: gender
   as Nike-style top tabs (MEN/WOMEN/KIDS-soon, defaults MEN), sport bands navigate
   in one tap (fixes the "can't move on" trap where gender chips hid below the fold).
   The ManualInput wizard itself is still to do.
   User floated email accounts — advised against: local-only app, nothing to sync,
   signup wall kills the funnel. Revisit only if distribution + backend ever land.
   _Update (same day)_: KIDS tab is now LIVE — Pro:Direct catalogue imported
   (`ed5dcab`: 744 entries, 529 new mens / 31 womens / 123 kids models, deduped from
   10.5k colourways via `scripts/import-prodirect.mjs`) and junior sizing shipped
   (`116f73d`: child 10K–13.5K + junior 1–5.5 size tables, kids-aware size lookup,
   ManualInput helper text). Imported entries carry real product photos (covers most
   of item 8) and direct Pro:Direct product links. Fit data inheritance: 492 entries
   from curated families, 191 standard defaults flagged "awaiting FootFit fit
   verification" in notes.
6. ✅ **SockSelection → brand accordion.** Shipped `d0aa713` as a left brand rail
   (ALL + brands, uppercase, active-bar indicator) + filtered sock list, search kept.
   Sock catalogue expanded `fdd3a3e`: 74 socks (31 football / 28 running / 15 rugby),
   thickness values are category-based estimates on the existing 0.2–0.9 mm scale.
   Result declutter shipped `237d6fa`: sort/width/brand chips behind a FILTERS toggle
   (green dot when active), surface picker images replaced with distinguishable
   texture close-ups, YOUR FIT header slimmed to one compact row.
7. **Result filter with fit counts.** Brand filter chips showing how many boots fit:
   "Adidas (3)". Extend `lib/bootListControls.ts`.
   _Related, shipped `d4cb8d8`+`60ee7f1`_: Adidas-style "match your surface" swipe
   picker on Result (football only) — All / FG / AG / SG / Turf / Indoor cards with
   imagery; centred card filters the list. Surface availability per boot captured
   from the Pro:Direct feed (`surfaces` field; curated non-feed entries default FG).

## P3 — Visual / asset-dependent

8. **Boot pictures** on each result card; tap → small gallery before the buy link.
9. **Sock pictures** in SockSelection — users recognise socks by sight, not name.
10. ✅ **Sport tiles: replace emojis** with live-action imagery. Shipped `ad9d5f0`
    (branch `feat/ui-nike-adidas`): Nike-style full-bleed photo bands, boot-level
    action shots (Unsplash CDN, offline fallback to dark band). Part of the wider
    Nike/Adidas redesign — black fit-profile hero card, squared chips, pill CTAs,
    uppercase label system, scan promo card for new users. Language documented in
    `.claude/rules/ui-rules.md`.
11. **Top navigation** (Home button + categories). Home redesign shipped with item 10;
    a persistent nav (Nike-style tab bar) deliberately deferred — the app is a linear
    fit-finder flow, tabs only earn their place if more destinations land (e.g. Saved /
    Profile). The 21 Nike/Adidas screenshots live in session `de923aaa…` for reference.

## Data expansion

12. **More socks** — as many as possible, the more detail the better. `/add-sock` batches.
13. ✅ **Rugby + running catalogues imported** (`7b691ee`, 2026-06-12): importer now
    sport-aware across all five Pro:Direct collections — DB is 1807 entries
    (720 football / 1041 running / 46 rugby). Rugby feed is ~90% cross-listed
    football boots; only "Rugby"-tagged titles + rugby-only brands enter as rugby.
    Running entries carry no surfaces field; explicit "Wide" variants get width
    class wide; toddler/infant sizes are rejected (under the table floor).
    959/1041 running entries have real product images (chips at item 8).
    Fit data: 124 inherited from curated families, 940 standard defaults flagged.

## Deferred / needs discussion

- **Daily automatic catalogue refresh** (user confirmed defer, 2026-06-12) — the
  bundled boot list updates by re-running `scripts/import-prodirect.mjs` on demand.
  Automate (scheduled job → static JSON on GitHub Pages → app fetches on launch)
  only once the app has users beyond the owner. Buy links always show live prices
  regardless.
- **"Sale" page** — requires price/commerce data the local-only app doesn't have.
  Revisit if a data source appears.
- **Multi-retailer price comparison on Buy** — same constraint: needs live pricing
  (affiliate APIs/backend). P1 item 3 (direct link) is the feasible slice now.

## Ambiguities to confirm during build

- Width-range numbers in item 2 were the user's off-the-cuff guesses — derive from
  actual width-estimation math.
- "Maximum length of the boot" in item 1 — interpret as the recommended size's
  internal length, not the largest size in the run.
